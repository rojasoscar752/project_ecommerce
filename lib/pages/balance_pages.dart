import 'package:flutter/material.dart';

class BalancePages extends StatelessWidget {
  const BalancePages({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}